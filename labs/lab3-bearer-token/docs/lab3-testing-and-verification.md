# Lab 3 testing and verification

The canonical deployment and validation guide is [docs/lab3-testing-and-verification.md](../../../docs/lab3-testing-and-verification.md).

Use that guide for the current managed-identity learner path, including:

- Unsigned Logic App invoke URL configuration
- Access-token claim validation without exposing the token
- Easy Auth success verification
- HTTP 401 and 403 diagnosis
- Application Insights and Logic App run-history evidence

This file intentionally contains no duplicate procedure so the active instructions cannot drift back to a SAS-signed callback URL.